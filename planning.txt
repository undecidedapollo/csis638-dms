RDTNative {
    constraints: string[]; // ["node"]
}

RDTFunction {
    name: "concat",
    params: [
        {
            type: "RDTParamMany",
            targetType: string,
        },
    ]
}

Transaction {
    id: string,
    bankAccountId: string,
    amount: number,
    doubleAmount: (rand: number) => $row.amount * 2 + rand
}

totalTransactions = () => {
    return Transaction[].reduce((acc, transaction) => {
        return acc + transaction.amount;
    }, 0);
}


Let tree compression / RDT Binding Compaction

Side effect representation as part of tree compression, ?? things that take actions need to split the tree so operations appear in order ??
